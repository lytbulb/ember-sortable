<article class="demo">
  <header>
    <h2>Ember Sortable</h2>
  </header>

  <main>
    <section class="vertical-demo flex-box">
      
      <div>
      	<div style="font-size: 11px;">
	      {{#each model.one as |item|}}
		  	{{item}} 
		  {{/each}}
		 </div>
		  {{#sortable-group owner="1" tagName="ol" model=model.one onMove="move" onAdd="add" onRemove="remove" as |group|}}
		    {{#each model.one as |item|}}
		      {{#sortable-item tagName="li" model=item group=group handle=".handle"}}
		        {{item}}
		        <span class="handle">
		          <span>&vArr;</span>
		        </span>
		      {{/sortable-item}}
		    {{/each}}
		  {{/sortable-group}}
	  </div>
      <div>
      	<div style="font-size: 11px;">
	      {{#each model.two as |item|}}
		  	{{item}} 
		  {{/each}}
		 </div>
		  {{#sortable-group owner="2" tagName="ol" model=model.two onMove="move" onAdd="add" onRemove="remove" as |group|}}
	        {{#each model.two as |item|}}
	          {{#sortable-item tagName="li" model=item group=group handle=".handle"}}
	            {{item}}
	            <span class="handle">
	              <span>&vArr;</span>
	            </span>
	          {{/sortable-item}}
	        {{/each}}
	      {{/sortable-group}}
      </div>
    </section>

  </main>

  <footer>
    <p>
      <a href="https://github.com/jgwhite/ember-sortable">View on GitHub</a>
    </p>
  </footer>
</article>
